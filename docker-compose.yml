services:
  pdf-tools:
    build:
      context: .
      args:
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-http://localhost:${HOST_PORT:-8088}}
    image: pdf-tools:latest
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-8088}:80"
